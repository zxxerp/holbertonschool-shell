#!/usr/bin/env bash
# Description: Groups visitors by IP and HTTP status code, then sorts by occurrences

awk '{count[$1" "$9]++} END {for (i in count) print count[i], i}' apache-access.log | sort -nr
