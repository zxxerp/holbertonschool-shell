#!/usr/bin/env bash
# This script displays username, UID, and home directory from /etc/passwd

while IFS=: read -r username password uid gid full_name home_dir shell; do
    echo "Username: $username, UID: $uid, Home: $home_dir"
done < /etc/passwd
